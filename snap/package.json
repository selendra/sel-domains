{
    "name": "@selendra/sns-snap",
    "version": "0.1.0",
    "description": "MetaMask Snap for Selendra Naming Service (.sel) domain resolution",
    "repository": {
        "type": "git",
        "url": "https://github.com/selendra/sns-snap.git"
    },
    "license": "(MIT-0 OR Apache-2.0)",
    "main": "./dist/bundle.js",
    "files": [
        "dist/",
        "images/",
        "snap.manifest.json"
    ],
    "scripts": {
        "build": "mm-snap build",
        "build:clean": "npm run clean && npm run build",
        "clean": "rimraf dist",
        "serve": "mm-snap serve",
        "start": "mm-snap watch",
        "test": "jest"
    },
    "dependencies": {
        "@metamask/snaps-sdk": "^6.0.0",
        "viem": "^2.21.0"
    },
    "devDependencies": {
        "@metamask/snaps-cli": "^6.0.0",
        "@metamask/snaps-jest": "^8.0.0",
        "@types/jest": "^29.5.12",
        "jest": "^29.7.0",
        "rimraf": "^5.0.7",
        "ts-jest": "^29.1.5",
        "typescript": "^5.5.2"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "publishConfig": {
        "access": "public",
        "registry": "https://registry.npmjs.org/"
    }
}